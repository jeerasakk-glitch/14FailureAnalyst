streamlit==1.32.0
pandas==2.2.0
openpyxl==3.1.2
chromadb==1.5.1
openai==1.12.0
anthropic==0.18.0
google-generativeai==0.4.0
httpx
fastapi
uvicorn
```

**3. แก้ Start Command บน Render:**
```
uvicorn app:app --host 0.0.0.0 --port $PORT
```

---

## Architecture สุดท้าย
```
Browser (HTML UI)
    │  POST /api/chat  {question, provider, api_key, search_mode}
    ▼
FastAPI (app.py) ← Render Service เดียว
    ├── ChromaDB embedded (./chroma_db/ จาก git)
    ├── RAG logic จาก 14Chat_r1.py
    └── เรียก DeepSeek / Anthropic / Gemini

